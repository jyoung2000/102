# Permissions (match your host user — Unraid defaults shown)
PUID=99
PGID=100
TZ=America/Chicago

# All other configuration is done via the Web GUI at http://localhost:1629
# Optional: pre-populate Baserow config on first launch
# BASEROW_API_URL=https://baserow.jymedia.cc
# BASEROW_API_TOKEN=your_token_here
# BASEROW_TABLE_ID=810
